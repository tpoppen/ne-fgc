# server

## TODO: 

- Add DynamoDB user integration
  - dynamo db client? 
  - or dynamo db document client?

- FIX: watch and dev start commands
      - Local docker is now working.
      - Need to fix watch command still, I think

- Add Permissions to JWT

- Migrate session to http only cookie?
  - need to figure out if i can still decode the jwt on the client when doing this
- Add Request JSON Schema Validation
- Add winston logger
