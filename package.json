{
  "name": "ne-fgc",
  "packageManager": "yarn@4.9.2",
  "type": "module",
  "scripts": {
    "build": "yarn workspaces foreach -Apvi --exclude root run build",
    "watch": "yarn workspaces foreach -Apvi --exclude root run watch",
    "start": "yarn workspace server run start",
    "start:prod": "yarn workspace server run start:prod"
  },
  "workspaces": [
    "src/client",
    "src/server"
  ],
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
