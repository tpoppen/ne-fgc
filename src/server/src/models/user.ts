
/**
 * User model: Takes DynamoDB Object response to build a User model
 * Properties:
 * - id
 * - username
 * - email
 * - nickname
 * 
 */
class User {
  constructor(userData: Object) {
      
  }
}

export default User;
